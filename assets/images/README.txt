━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
배경 이미지 추가 방법
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

이 폴더(assets/images/)에 PNG 또는 JPG 파일을 넣으면 됩니다.

파일 이름 규칙:
    bg_stage_01.png  →  Stage 01 (Lab Battlefield) 배경
    bg_stage_02.png  →  Stage 02 (Vertical Lab) 배경
    bg_stage_03.png  →  Stage 03 (The Abyss) 배경
    bg_stage_04.png  →  Stage 04 (Flat Zone) 배경
    bg_default.png   →  위 파일이 없을 때 기본 배경

권장 해상도: 1280 × 720 (자동 스케일되므로 다른 크기도 OK)
지원 형식  : PNG, JPG (PNG 권장)

이미지 파일이 없으면 절차적 배경(그라디언트+별+도시)이 사용됩니다.
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
